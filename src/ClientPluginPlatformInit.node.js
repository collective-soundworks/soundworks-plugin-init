import { ClientPlugin } from '@soundworks/core/client.js';

// dummy plugin so that more code can be shared between node and browser runtimes
export default class ClientPluginPlatformInit extends ClientPlugin {}
